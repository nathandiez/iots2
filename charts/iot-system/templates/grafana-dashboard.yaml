apiVersion: v1
kind: ConfigMap
metadata:
  name: iot-system-dashboard
  namespace: {{ .Values.namespace }}
  labels:
    grafana_dashboard: "1"
    {{- include "iot-system.labels" . | nindent 4 }}
data:
  iot-system-dashboard.json: |-
{{ .Files.Get .Values.grafana.dashboardFile | indent 4 }}
